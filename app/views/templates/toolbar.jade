- svgHex = function(x, y, r) {
-	points = "";
-	for(var i = 0; i < 6; i++) {
-		angle = 2 * Math.PI / 6 * (i + 0.5);
-		points += "" + (x + r * Math.cos(angle)) + ",";
-		points += "" + (y + r * Math.sin(angle)) + " ";
-	}
-	return points;
- }

svg#build(viewBox='0 0 5 2', preserveAspectRatio='xMinYMin meet', width="100%", height="100%")
	g.progress(transform="translate(1,1)")
		polygon(fill='none', stroke="#8BD14E", stroke-width='0.05', points=svgHex(0, 0, 1))
		text(y="-0.25em", fill="#ffffff", style="font-size:0.5px") 0
		text(y="0.85em", fill="#ffffff", style="font-size:0.5px") -

	g.resource-view(transform="translate(4.5,1)")
		polygon(fill="#8BD14E",points=svgHex(0, 0, 1),transform="scale(2.5, 1)")
		text(y="0em", fill="#ffffff", style="font-size:0.5px") Resource View
		text(y="1.1em", fill="#ffffff", style="font-size:0.4px") [Space]
		

